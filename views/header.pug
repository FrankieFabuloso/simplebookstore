navnavbar.navbar-default.navbar-fixed-top
  .container-fluid
    a.navbar-brand(href='/')
      img(id='brand', alt='Brand', src='/images/bookBrand.png')
      span BookStore
    .navbar-header
      ul.nav.nav-tabs
        li(role='presentation' class={active: isAdmin})
          a(href='/admin') Admin
        li(role='presentation' class={active: isBookList})
          a(href='/') Book List
        if isAdmin
          li(role='presentation')
            a(href='#',type='button', data-toggle='modal', data-target='#newBook')
              | Add Book
