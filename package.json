{
  "name": "simplebookstore",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon ./bin/www",
    "build": "export DBNAME='simpleBookstore' && dropdb simpleBookstore -e -i --if-exists && createdb simpleBookstore && psql simpleBookstore < ./database/schema.sql && psql simpleBookstore < ./database/books.sql"
  },
  "dependencies": {
    "body-parser": "~1.15.2",
    "cookie-parser": "~1.4.3",
    "debug": "~2.2.0",
    "express": "~4.14.0",
    "morgan": "~1.7.0",
    "nodemon": "^1.11.0",
    "pg-promise": "^5.4.7",
    "pug": "~2.0.0-beta6",
    "serve-favicon": "~2.3.0"
  }
}
